
if (!canSuspend) exitWith {
 	
};

sleep 0.5;

// [player] spawn FST_jumppack_fnc_add_arsenal_handlers;
// [player] spawn FST_jumppack_fnc_add_cba_per_frame;
// call FST_jumppack_fnc_add_cba_player_loadout_EH;

// sleep 0.5;

// call FST_jumppack_fnc_keybinds;
