# Isolated downtime diagnostics

These are test-only fixtures and lab functions, not deployable replacements. See ../../docs/downtime-2026-09-24/CONCLUSIONS.md and COMPARISON_ERRATA.md.

Git copies have normalized line endings and trailing whitespace. Exact executed fixture PBOs, raw logs, byte-preserved lab source, build identities and SHA-256 manifests are retained in the separate AAT-Downtime-2026-09-24-Tests.zip evidence package. Rebuilding these normalized copies may produce different byte hashes without a behavior change.
